# Datasets Directory (`ml-learning/datasets`)

This folder stores small datasets, dataset schema documentations, and synthetic data generation scripts for Machine Learning tasks.

## Large Data Policy
- Small tabular files (< 1MB) used in unit tests or quick tutorials may be tracked.
- Medium/Large CSV, Parquet, JSONL, or image files are automatically excluded from Git tracking via `.gitignore`.
